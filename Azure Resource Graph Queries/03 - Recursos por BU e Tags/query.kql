// Para filtrar por subscriptions, insira após a linha "Resources":
// | where subscriptionId in (
//     'SUBSCRIPTION-ID-1',
//     'SUBSCRIPTION-ID-2'
// )

Resources
| where type =~ "microsoft.compute/disks"
| join kind=leftouter (
    ResourceContainers
    | where type =~ "microsoft.resources/subscriptions"
    | project subscriptionId, subscriptionName = name
) on subscriptionId
| project-away subscriptionId1
| extend diskState = tostring(properties.diskState)
| extend attachedTo = tostring(managedBy)
| extend isUnattached = iff(isempty(attachedTo) or diskState =~ "Unattached", "SIM", "NAO")
| extend criadoEmTag = coalesce(tostring(tags["Criado em"]), tostring(tags["criado em"]), tostring(tags["CriadoEm"]), tostring(tags["createdOn"]))
| project
    subscriptionName,
    subscriptionId,
    resourceGroup,
    diskName = name,
    location,
    skuName = tostring(sku.name),
    skuTier = tostring(sku.tier),
    diskState,
    isUnattached,
    attachedTo,
    osType = tostring(properties.osType),
    diskSizeGB = tostring(properties.diskSizeGB),
    diskIOPSReadWrite = tostring(properties.diskIOPSReadWrite),
    diskMBpsReadWrite = tostring(properties.diskMBpsReadWrite),
    maxShares = tostring(properties.maxShares),
    networkAccessPolicy = tostring(properties.networkAccessPolicy),
    publicNetworkAccess = tostring(properties.publicNetworkAccess),
    zones = tostring(zones),
    createdAt = tostring(properties.timeCreated),
    systemCreatedAt = tostring(systemData.createdAt),
    createdBy = tostring(systemData.createdBy),
    criadoEmTag,
    tags = tostring(tags),
    id
| order by isUnattached desc, subscriptionName asc, resourceGroup asc, diskName asc
